{% extends 'base.html.twig' %}

{% block title %}Modifier {{ course.title }}{% endblock %}

{% block body %}
<nav aria-label="breadcrumb" class="mb-4">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('course_index') }}">Cours</a></li>
        <li class="breadcrumb-item"><a href="{{ path('course_show', {id: course.id}) }}">{{ course.title }}</a></li>
        <li class="breadcrumb-item active">Modifier</li>
    </ol>
</nav>

<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="card shadow-sm">
            <div class="card-header bg-white py-3">
                <h4 class="mb-0 text-primary"><i class="bi bi-pencil me-2"></i> Modifier le cours</h4>
            </div>
            <div class="card-body">
                {{ include('course/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
